\chapter{Implementing a formatter}

% TODO we'll write this in JS
% no point in using e.g. ANTLR, because we need no parser and the lexer is pretty trivial
% describe implementation with code snippets
% code examples both from earlier in the paper and from external lisp code
